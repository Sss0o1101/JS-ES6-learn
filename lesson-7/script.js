/*  数値を受け取って処理 ------------------------------------------------------------------------------------------*/

    // //Number()  //parseInt()

    // const n = Number(prompt('Number?'));

    // console.log(n + 5);


/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/*  小数点を含む数値を処理しよう ------------------------------------------------------------------------------------------*/

    // //Math.ceil・・・切り上げ  //Math.floor・・・切捨て
    // //Math.round・・・中間点

    // const n = Number(prompt('Minutes?'));

    // console.log(`Hours: ${Math.floor(n / 60)}`);
    // console.log(`Minutes: ${n % 60}`);

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/*  数値を見やすく表示しよう ------------------------------------------------------------------------------------------*/

    // //.toFixed()・・・少数点以下 2 桁までを表示
    // //.toLocaleString()・・・3 桁ごとにカンマ

    // const n = Number(prompt('Yen?'));
    // const amount = (n / 138.91).toFixed(2);  // Number((n / 138.91).toFixed(2)); でもいい

    // console.log(`USD: ${Number(amount).toLocaleString()}`);  //1ドル = 138.91

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/
