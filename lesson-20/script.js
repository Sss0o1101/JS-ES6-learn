/* (HTMLを書き換える)  ------------------------------------------------------------------------------------------*/

    document.querySelector('#choice').textContent = new Date(); // 日時を表示

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (if else)  ------------------------------------------------------------------------------------------*/

    if (window.confirm('ゲームスタート！準備はいい？')) {
        console.log('ゲームを開始します。');
    } else {
        console.log('ゲームを終了します。');
    }

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (変数, 定数)  ------------------------------------------------------------------------------------------*/

    let answer = window.prompt('ヘルプを見ますか？');
    console.log(answer);

    const answers = window.prompt('ヘルプを見ますか？');
    if (answers === 'yes') {
        window.alert('タップでジャンプ、障害物を避けます。')
    }

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (else, if)  ------------------------------------------------------------------------------------------*/

    const answers2 = window.prompt('ヘルプを見ますか？');
        if (answers2 === 'yes') {
            window.alert('タップでジャンプ、障害物を避けます。')
        } else if (answers2 === 'no') {
            window.alert('ゲーム起動中')
        } else {
            window.alert('yesかnoでお答えください')
        }

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (数当てゲーム)  -------------------------------------------------------------------------------------------------------------------------------*/

    // //Math.floor・・・切り捨て  //parseInt(変換したい文字列)  //let 変数名;・・・先に変数の定義 (if文などで代入されるデータが変わる場合)


    const number = Math.floor(Math.random() * 6);
    const answer3 = parseInt(window.prompt('数当てゲーム。0~5の数字を入力してね'));

    let message;
    if (answer3 === number) {
        message = 'あたり！';
    } else if (answer3 < number) {
        message = '残念でした！もっと大きい';
    } else if (answer3 > number) {
        message = '残念でした！もっと小さい';
    } else {
        message = '0~5の数字を入力してね';
    }

    window.alert(message);

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (時間で異なるメッセージを表示する)  -------------------------------------------------------------------------------------------------------------------------------*/

    //new Date()・・・現在の時刻を取得する  //getHours()・・・ページを開いた「時」を24時間で取得して定数hourに代入

    const hour = new Date().getHours();

    if (hour >= 19 && hour < 21) {
        window.alert('お弁当30%OFF!')
    } else if (hour === 9 || hour === 15) {
        window.alert('お弁当1個買ったら1個おまけ')
    } else {
        window.alert('お弁当はいかがですか')
    }

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* ( while文 (1枚、2枚、3枚 と出力する))  -------------------------------------------------------------------------------------------------------------------------------*/

    let i = 1
    while(i <= 10) {
        console.log(`${i}枚`);
        i = i + 1;
        // i += 1
    }

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (コンソールでモンスターを倒せ！)  -------------------------------------------------------------------------------------------------------------------------------*/

    let enemy = 100;
    let count = 0;
    window.alert('戦闘スタート');

    while(enemy > 0) {
        const attack = Math.floor(Math.random() * 30) +1;
        console.log(`モンスターに${attack}のダメージ`);
        enemy -= attack;
        count += 1;
    }
    console.log(`${count}回でモンスターを倒した!`);

/* ----------------------------------------------------------------------------------------------------------------------------------------------*/


/* (税込価格を計算する)  -------------------------------------------------------------------------------------------------------------------------------*/

    function total (price) {
        const tax = 0.1;
        return price + price * tax;
    };
